# 开发日志 — 2026-06-18

## 今日事项
- 搭建最小可运行的项目骨架（Python 后端 + Vue 前端）。
- 配置 DeepSeek API Key 并验证调用成功。
- 完成前后端联调，实现对话交互与对话记录存储。

## 今日完成

### 后端 (`src/backend/`)
- [x] 搭建 FastAPI 项目骨架：
  - `main.py` — FastAPI 应用入口，挂载路由与中间件。
  - `app/config.py` — 集中管理配置（数据库连接、DeepSeek API Key 等）。
  - `app/models.py` — 定义 SQLAlchemy 数据模型（`ChatSession`、`ChatMessage`）。
  - `app/llm_client.py` — 封装 DeepSeek API 调用（基于 `openai` SDK）。
  - `app/routers/chat.py` — 聊天相关接口：创建会话、发送消息、流式响应、获取历史记录。
- [x] 配置 `.env` / `.env.example`，管理敏感信息与数据库连接。
- [x] 编写 `requirements.txt`，锁定核心依赖（FastAPI、SQLAlchemy、PyMySQL、`openai` 等）。
- [x] 编写 `test_chat.py`，本地验证 DeepSeek API 调用是否正常。

### 前端 (`src/frontend/`)
- [x] 使用 Vite + Vue 3 搭建前端项目骨架：
  - `package.json` — 依赖：`vue`、`vue-router`、`ant-design-vue`，构建工具：`vite`、`@vitejs/plugin-vue`。
  - `vite.config.js` — Vite 基础配置。
  - `src/main.js` — 应用入口，注册 Vue Router 与 Ant Design Vue。
  - `src/App.vue` — 根组件。
  - `src/router/index.js` — 路由配置。
- [x] 开发首页对话界面 `src/views/HomeView.vue`：
  - 支持用户输入与 AI 回复展示。
  - 支持流式输出（打字机效果）。
  - 支持新建会话、切换会话、展示历史消息。

### 数据库 (`src/database/`)
- [x] 编写 `sql/init.sql`，初始化 MySQL 数据库：
  - 创建 `chat_sessions` 表（会话信息）。
  - 创建 `chat_messages` 表（消息记录，含角色、内容、时间戳）。

### 联调与验证
- [x] 后端服务正常启动，API 文档可访问（`http://localhost:8000/docs`）。
- [x] 前端 `npm run dev` 正常启动，可访问首页。
- [x] 前端可正常调用后端接口，与 DeepSeek 大模型进行实时对话。
- [x] 对话内容与对话记录可正常写入 MySQL 数据库，历史记录可查询展示。

## 待办 / 下一步
- [ ] 开始功能 1（古诗文默写出题）的技术设计与代码实现。
- [ ] 完善错误处理与边界场景（网络异常、API 限流、空输入等）。
- [ ] 优化前端交互体验（加载状态、错误提示、响应式布局）。

## 备注
- 今日完成了从 0 到 1 的全栈骨架搭建，前后端与数据库均已跑通。
- 核心链路验证完毕：前端 → 后端 → DeepSeek API → 数据库存储，整个流程可用。
